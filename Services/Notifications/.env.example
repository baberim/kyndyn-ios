# Identifiers are configuration, not secrets. Values are documented here so
# Cloudflare setup is reproducible without storing private signing keys.
APPLE_TEAM_ID=96TZPZ6H89
APNS_TOPIC=com.kyndynfamily.kyndyn
APNS_SANDBOX_KEY_ID=2K8PCHCCV8
APNS_PRODUCTION_KEY_ID=G6L8MSV9F4

# Never add either APNs .p8 value here. Store them as encrypted Worker secrets:
# APNS_SANDBOX_PRIVATE_KEY
# APNS_PRODUCTION_PRIVATE_KEY

# Generate independently with a cryptographically secure 32-byte random value.
# Store only as an encrypted Worker secret.
# DEVICE_TOKEN_ENCRYPTION_KEY
